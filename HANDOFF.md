# Аукцион лута — статус на 2026-08-27

Telegram Mini App: админ грузит скриншоты внутриигрового аукциона, приложение
режет их на лоты по иконке предмета, определяет цвет редкости, пользователи
делают ставки, админ разыгрывает победителей с лимитами по цвету. OCR и цена
полностью выпилены (не читаем текст со скриншота вообще).

## Где что физически работает

- **Живой сервер** запускается из `C:\Users\zelik\Documents\клуд` (ветка
  `master`, НЕ этот воркспейс) командой `npm start`. Раздаётся наружу через
  **ngrok**: `ngrok http 3000 --url=viewable-constrict-amplify.ngrok-free.dev`.
  Оба процесса поднимаются вручную после перезагрузки компьютера — они никак
  не связаны с этой Claude-сессией.
- **Этот воркспейс** — `.claude\worktrees\hello-fb74f7`, ветка
  `claude/hello-fb74f7`. Рабочее место для правок и тестов. Изменения
  делаются здесь, гоняются тесты (`npx vitest run` + `npx tsc --noEmit`),
  потом **вручную копируются** (`cp`) в `C:\Users\zelik\Documents\клуд` — это
  НЕ git merge, просто копирование файлов. После копирования backend-правок
  сервер нужно перезапускать (frontend-only правки подхватываются на лету,
  рестарт не нужен — `dist/web` читается с диска на каждый запрос).
- На 2026-08-27 воркспейс и живая папка **синхронизированы** — обе стороны
  содержат одинаковый код (дедуп между отдельными загрузками скриншотов,
  сворачиваемый список победителей, фикс удаления ивента — см. ниже). Раньше
  они разъезжались: в одной из прошлых сессий фичи дедупа/списка победителей
  скопировали в живую папку, но не занесли обратно в воркспейс — это было
  исправлено сейчас копированием в обе стороны.
- Есть ещё "чужие" воркспейсы от прошлых сессий: `telegram-loot-auction-app-
  abcb3a` и `loot-auction-fixes-af5666`, плюс Remote-Control сессии
  ("Auction UI functionality" и др.) — все они сейчас числятся offline, но
  их процессы на дисках (в т.ч. на других устройствах) могли остаться
  запущенными независимо от статуса самой Claude-сессии. Если в одном из них
  жив свой `npm start` — он либо перехватит порт 3000, либо (что сейчас и
  происходит) начнёт конфликтовать за Telegram bot polling. См. "Открытый
  блокер" ниже.

## Стек

Fastify + better-sqlite3 + sharp + ваниль TS/Vite фронтенд, Telegraf-бот.
Auth — Telegram initData (HMAC).

## Важная особенность машины

На этой Windows-машине **нет инструментов сборки** (Visual Studio Build
Tools). `npm install`/`npm uninstall` без явной нужды может попытаться
пересобрать нативные модули (`better-sqlite3`) и **снести node_modules
целиком** — уже случалось. Ставить новые зависимости — только по одной,
сразу проверяя, что тесты/сборка не сломались. **Не запускать install/
uninstall без явного запроса пользователя** — было отдельно оговорено.

Также у better-sqlite3 в этой сборке **внешние ключи включены по умолчанию**
(`PRAGMA foreign_keys = 1`), в отличие от типичного поведения SQLite. Это
реальная причина бага ниже — при написании новых DELETE-транзакций держать
это в уме и чистить все зависимые таблицы, а не полагаться на то, что FK
выключены.

## Схема БД (текущая)

`users`, `events`, `screenshots` (event_id, original_path, rows, template,
uploaded_by), `items` (event_id, screenshot_id, color, quantity, image_path,
status), `claims`, `item_winners` (item_id, telegram_id — лот с quantity > 1
разыгрывает несколько разных победителей из тех, кто на него ставил).
`name`/`price` в `items` — мёртвые колонки с дефолтом, оставлены схемой, но
никем не читаются. Таблиц `settings`/`price_tiers`/`catalog_items` больше
нет.

Миграция в `src/server/db.ts` — не пошаговая, а drop+recreate: при
обнаружении старых колонок (`catalog_item_id`, потом `winner_telegram_id`)
таблицы `items`/`screenshots`/`claims`/`item_winners` дропаются и создаются
заново с текущей схемой. Осознанно, т.к. продакшен-данных пока нет.

## Правило лимитов побед (зашито в коде, не настраивается)

В `src/server/routes/events.ts`: фиолетовый+красный вместе — максимум **1**
победа на человека за ивент, синий — максимум **2**. Ставок можно делать
сколько угодно, лимит только на выигрыш.

## Дедуп лотов по иконке (`src/server/dedup.ts`)

Иконки сравниваются через downscaled-thumbnail фингерпринт (16×16, mean
per-byte diff, порог 8 — откалибровано на реальном скриншоте 2026-08-27).
Дедуп работает в два прохода: сначала группирует повторы внутри одной
загрузки (`groupBySignature`), затем отдельно сверяет каждую итоговую группу
с тем, что уже лежит в пуле этого ивента (`isSameIcon`) — так ловятся
повторы **между разными** загрузками скриншотов, а не только внутри одного
запроса. При совпадении лоту просто увеличивается `quantity`, новый лот не
создаётся.

## Список победителей на странице юзера (`web/views/pool.ts`)

Свёрнут по умолчанию в `<details>/<summary>` — «Победители (N)», без JS,
чистый HTML/CSS. Бэкенд отдаёт `item.winners[]` через `attachWinners` в
`events.ts`.

## Калибровка распознавания (`src/server/layout-templates.ts`)

Координаты для шаблонов `feast` (Пир победы) и `invasion` (Аукцион
вторжения) подобраны на глаз по реальным скриншотам пользователя.
`contentTop` — какую долю скриншота сверху пропустить перед нарезкой на
строки: 0.43 для feast, 0.5 для invasion. Если распознавание систематически
промахивается — спросить у пользователя конкретный пример и подправить
координаты в этом файле.

## Исправлено 2026-08-27: 500-ошибка при удалении ивента

`DELETE /events/:id` чистил `claims`, `items`, `screenshots`, но не
`item_winners`. Раз в этой сборке better-sqlite3 внешние ключи включены по
умолчанию, удаление `items` при висящих ссылках из `item_winners` падало с
`FOREIGN KEY constraint failed` → голый 500 "Internal Server Error" на
клиенте. Воспроизведено тестом, поправлено добавлением `DELETE FROM
item_winners WHERE item_id IN (...)` в ту же транзакцию — см. `events.ts`.
Пофикшено и в воркспейсе, и в живой папке; 66/66 тестов зелёные,
`tsc --noEmit` чист в обоих местах.

## Открытая проблема: нестабильная загрузка скриншотов

Симптом: при загрузке реальных скриншотов с телефона через ngrok — ошибка
503 / "Premature close". Диагностировано: файл физически не долетает до
сервера — дело не в размере файла и не в скорости обработки, а в
нестабильности/медленности канала телефон↔ngrok.

Уже сделано (`web/views/eventDetail.ts`, `src/server/routes/screenshots.ts`):
ответ отдаётся сразу после нарезки (цвет — в фоне), загрузка по одному файлу
за запрос, сжатие на клиенте (JPEG, ширина до 720px, качество 0.7),
автоповтор до 3 попыток. Снижает вероятность обрыва, но не гарантирует
результат при по-настоящему плохой сети. Рекомендовано: пробовать через
Wi-Fi, при необходимости — переезжать с "локальный комп + ngrok" на
нормальный хостинг (конфиги есть в `deploy/`).

## Открытый блокер (2026-08-27, не решено): живой сервер не поднимается

При `npm start` в `C:\Users\zelik\Documents\клуд` процесс falls over сразу
после старта HTTP-части (порт 3000 успевает ответить 200, но затем крашится
целиком):

```
TelegramError: 409: Conflict: terminated by other getUpdates request;
make sure that only one bot instance is running
```

Значит **прямо сейчас где-то ещё** активно опрашивает Telegram другой
процесс с тем же `BOT_TOKEN`. На этой машине (DESKTOP-K3SPAF7) других
node-процессов, похожих на этот бот, не найдено — единственный посторонний
`node.exe` (PID менялся между проверками) держит ~20 установленных
соединений на порт 8886 к разным внешним IP, что не похоже на Telegram Bot
API (тот ходит на `api.telegram.org:443`) и на сам этот бот не похоже —
трогать его не стали, но стоит отдельно проверить, что это за процесс на
машине (возможно не относится к проекту вообще).

Повторили попытку `npm start` дважды (с паузой) — оба раза одна и та же
ошибка сразу, не "залипший" 50-секундный polling-слот. Значит конфликтующий
инстанс либо на другом устройстве (телефон/другой ПК из старых
Remote-Control сессий — они числятся offline, но их процессы могли
остаться жить), либо где-то ещё на этой же машине под процессом, который
не отловился как `node.exe`.

**Как продолжить:** найти и остановить тот второй инстанс (проверить другие
устройства/старые терминалы, где раньше запускали `npm start` для этого же
бота), затем просто `npm start` в `C:\Users\zelik\Documents\клуд` — код уже
готов и протестирован, ждать больше нечего кроме освобождения токена.

## Как продолжать

1. Синхронизировать состояние: `git status --short` в обоих местах (воркспейс
   и `C:\Users\zelik\Documents\клуд`) — после сегодняшнего слияния файлы
   идентичны по содержимому, но в воркспейсе изменения пока не закоммичены
   (коммитить только по явной просьбе).
2. Перед любым рестартом живого сервера — сначала убедиться, что старый
   процесс действительно мёртв (`curl http://127.0.0.1:3000/`), и что никакой
   другой инстанс этого бота не молотит `getUpdates` (см. блокер выше) —
   иначе получите тот же 409.
3. После правок backend — обязательно `npx vitest run` + `npx tsc --noEmit` в
   обоих местах перед копированием и рестартом.
4. `npm install`/`npm uninstall` — только по явному запросу пользователя, на
   этой машине это рискованная операция (см. "Важная особенность машины").
