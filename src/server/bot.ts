import { Telegraf, Markup } from 'telegraf';

const INSTRUCTIONS = `📋 Как пользоваться ботом

1. Жми «Открыть» — попадёшь в мини-приложение.
2. Впиши свой игровой ник (один раз) — после этого заявка уходит на одобрение админу. Пока не одобрят, доступа к лотам не будет.
3. Как одобрят — открывай вкладку «Лоты»: там список трофеев с текущего ивента.
4. Перед стартом будет обратный отсчёт «Аукцион начнётся через…» — держи вкладку открытой, лоты появятся сами, ничего обновлять руками не нужно.
5. Жми «Ставка», чтобы забрать лот — кто первый нажал, тот и получил. На некоторых синих лотах во «Вторжении» доступен выбор «−1+», если лимит позволяет забрать сразу 2 шт.
6. Пока приём заявок не закончился, ставку можно отменить кнопкой «Отменить».
7. Фиолетовый лот во «Вторжении» можно забрать 1 раз в сутки (замы — 2 раза), независимо от количества ивентов в этот день. Но за один конкретный ивент фиолетовый — максимум 1 штука для всех, включая замов: 2 в сутки — это про разные ивенты, а не про то, что можно забрать сразу два фиолетовых в одном.
8. Когда приём заявок закончится, лоты, которые никто не занял, уходят в «Свободные» внизу списка — забрать их в приложении уже нельзя, за них бьются по правилам аукциона прямо в игре.`;

export function createBot(botToken: string, miniAppUrl: string) {
  const bot = new Telegraf(botToken);
  bot.start((ctx) =>
    ctx.reply(
      'Открыть аукцион лута',
      Markup.inlineKeyboard([[Markup.button.webApp('Открыть', miniAppUrl), Markup.button.callback('Инструкция', 'instructions')]])
    )
  );
  bot.action('instructions', async (ctx) => {
    await ctx.answerCbQuery();
    await ctx.reply(INSTRUCTIONS);
  });
  return bot;
}
